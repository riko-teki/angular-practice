import { GenericMasterPipe } from './generic-master.pipe';

describe('GenericMasterPipe', () => {
  it('create an instance', () => {
    // const pipe = new GenericMasterPipe();
    // expect(pipe).toBeTruthy();
  });
});
