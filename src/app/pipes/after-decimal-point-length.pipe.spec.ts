import { AfterDecimalPointLengthPipe } from './after-decimal-point-length.pipe';

describe('AfterDecimalPointLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new AfterDecimalPointLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
